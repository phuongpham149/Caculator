<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ButtonInCaculator">
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/caculator_btn_text_size</item>
        <item name="android:background">@drawable/animationbutton</item>
        <item name="android:textColor">@color/caculator_btn_text_color</item>
    </style>
</resources>
